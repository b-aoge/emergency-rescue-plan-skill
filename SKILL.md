---
name: 现场应急救援预案
description: 编制房屋建筑、市政、工业厂房等建设工程施工项目《施工现场应急救援预案》docx 文档的工作流。当用户需要为某个施工项目起草、生成或完善"应急救援预案""事故应急预案""现场应急处置方案""应急抢险预案"等现场安全管理文档时使用。覆盖依据 GB/T 29639-2020 的标准 9 章 + 附件结构、可复用附件正文库、python-docx 自动排版引擎（含目录静态真实页码、全黑字、图片同页等坑点），以及面向项目经理的个性化信息收集清单。
agent_created: true
---

# 现场应急救援预案 编制工作流

本技能把一个施工项目的《施工现场应急救援预案》从零散需求生成成一份排版规范、可直接打印/报审的 Word（docx）文档。核心交付物是一个 `.docx` 文件（约 35–40 页）。

## 何时使用

- 用户说"帮我写/生成/完善 XX 项目的应急救援预案""出一份事故应急预案""现场应急处置方案"等。
- 用户发来一份旧预案要求按施工组织设计/模板对齐、去重、补信息、压缩页数。
- 前提是"施工项目"场景（房建、市政、工业厂房、安装等）；非施工类（如工厂、学校）的预案结构不同，需另议。

## 标准文档结构（9 章 + 附件）

依据《生产经营单位生产安全事故应急预案编制导则》GB/T 29639-2020 与施工行业惯例，正文固定为以下 9 章，目录条目与之严格一一对应：

1. **一、编制依据** — 法律法规 + 标准规范清单（安全生产法、建筑法、建设工程安全生产管理条例、生产安全事故应急条例、GB/T 29639-2020、JGJ46/59/80、GB50497、GB50016、危险化学品安全管理条例、施工图纸与施组、项目所在地规定等）。
2. **二、基本原则与方针** — "安全第一、预防为主、综合治理、自救为主、统一指挥、分工负责"；优先保护人和大多数人。
3. **三、工程项目基本情况** — 工程总体概述（名称、地点、建设/设计/勘察/监理/施工单位、规模、结构、工期、承包方式、危大工程等）+ 现场医疗设施 + 消防救助设施。
4. **四、可能发生事故的确定及主要危险源** — 列出本项目可能的事故类型（坍塌、高处坠落、物体打击、起重伤害、机械伤害、触电、火灾爆炸、中毒窒息、车辆伤害、高温台风等）及主要危险源。
5. **五、应急机构的组成、责任和分工** — 领导小组（组长/副组长/组员）+ 应急联络一览表（姓名/职务/电话）+ 指挥流程图 + 职责分工。
6. **六、报警信号与通讯** — 报警电话清单（120/119/110/122/12369/12350）+ 报警注意事项 + 报警救援联络电话表。
7. **七、应急响应与救援** — 一般/重大事故应急响应、报告时限（1 小时上报）、现场应急救援措施表（事故类别/原因/措施/演练时间）+ 演练评价修改。
8. **八、有关规定和要求** — 9 条岗位责任制与演练学习要求。
9. **九、附件** — 附件1 常见事故急救常识、附件2 火灾逃生自救知识、附件3 灭火器的使用常识、附件4 各类事故的预防措施、附件5 施工总平面布置图、附件6 应急救援路线图。

附件 1–4 的正文是高度可复用的行业通识文本，已存于 `references/attachments_bank.md`，可直接复制到 `plan_part4.md` 使用；仅需根据项目工艺属性微调（如危化品项目补 VOC/有限空间内容）。

## 工作流（5 步）

### 第 1 步：收集个性化信息（必做，先问再写）

不要凭空编造。先向用户确认以下信息，缺失时须明确说明"以下为占位/示例，请按实填写"：

- 工程基本信息：工程名称、建设地点、建设单位、设计单位、勘察单位、监理单位、总承包/施工单位、建筑面积/规模、结构类型、工期、承包方式（总包/甲供等）。
- 项目人员（用于应急领导小组与联络表）：项目经理（组长）、技术负责人、专职安全员、施工负责人、设备/材料负责人、保卫负责人的姓名与电话。
- 就近医院：名称、电话、与工地的路线距离（用于附件6 路线图与第六章电话表）。
- 公司应急办公室电话。
- **关键场景判定（影响正文口径，务必确认）**：
  - 现场**是否有宿舍**？无宿舍则删除所有"宿舍"相关表述，改为"现场办公区/临时休息区"。
  - 是否涉及**超危大/需专家论证**的分部分项工程？无则删除"专家论证"等字样。
  - 是否有**危化品/有限空间**属性（如本例异辛酸项目）？有则补 VOC、有限空间、易燃易爆章节内容。
- 附图：施工总平面布置图（PDF/图片）、应急救援路线图（高德/百度地图截图）是否提供。

> 参考 `references/example_project.md`：本例（年产5万吨异辛酸灌装站土建工程）的完整问答与特殊处理，可作为同类工业项目的核对样板。

### 第 2 步：拆分源文件

将内容拆分为 `plan_part1.md`～`plan_part5.md`（放在工作目录），由构建脚本顺序拼接：

- `plan_part1.md`：封面块（`<!-- cover -->`…`<!-- /cover -->`）+ 分隔符 `---` + 第一、二、三章。
- `plan_part2.md`：第四、五、六章。
- `plan_part3.md`：第七章（含措施表、演练时间）。
- `plan_part4.md`：第八章、第九章目录、附件1–4（直接复制 `references/attachments_bank.md` 并按项目微调）。
- `plan_part5.md`：附件5（平面图，含 `![...](图路径)`）、附件6（路线图）。

源文件语法约定（被 `scripts/build_docx.py` 解析）：

- 封面：`# 标题` / `## 副标题` / `编制：` `审核：` `批准：` / `编制单位：` `编制日期：`，用 `<!-- cover -->` 与 `<!-- /cover -->` 包裹。
- 章节：`# 一、编制依据` 为一级标题；`## 1．工程总体概述` 为二级；`### x．...` 为三级。
- 分隔符 `---`：封面后的第一个 `---` 触发插入目录；后续 `---` 画章节分隔线。
- 表格：以 `|` 开头的连续行，首行表头、第二行分隔、其后数据；列宽按表头特征自动匹配（3 列联络/2 列电话/5 列措施）。
- 图片：`![说明文字](相对路径)`，平面图最大 11cm、含"路线"字样的最多 8cm（手机长截图要更窄才能与标题同页）。
- 强制分页：`<!-- pagebreak -->` 单独成行。

### 第 3 步：构建 docx

运行 `scripts/build_docx.py`（需要 office venv，见下"环境"）：

```powershell
cd <工作目录>
& "C:\Users\a'su's\.workbuddy\binaries\python\envs\office\Scripts\python.exe" "<skill目录>\scripts\build_docx.py"
```

脚本读取当前目录的 `plan_part1~5.md`，输出 `施工现场应急救援预案_生成版.docx`。已内嵌的排版规则：

- 全文字体黑色，标题黑体、正文宋体（符合国内施工资料报审规范，且用户常要求"不要彩色字"）。
- 封面签名区（编制/审核/批准）水平居中、置于页面中下部，下划线留白供手写。
- 目录独立一页；条目右侧点线引导 + **静态页码**（若同目录存在 `toc_pages.json`）。
- 页脚居中"第 N 页"自动页码。
- 表格：100% 版心、固定列宽、表头蓝底（D9E2F3）、单元格垂直居中。
- 标题 `keep_with_next`，图片 `keep_with_next`，避免"标题在页尾、图在下一页"。

### 第 4 步：计算并写死真实页码（关键坑点）

腾讯文档/在线预览**不会刷新 Word 域**（PAGEREF），直接显示占位值（如全显示"2"）。务必用 Word COM 先算真实页码再写死：

1. 先以 PAGEREF 模式构建一次（无 `toc_pages.json` 时脚本自动用 PAGEREF）。
2. 运行 `scripts/compute_pages.py`，用 Word 打开文档、更新域、读取每个 `_TocN` 书签的真实页码，写入 `toc_pages.json`。
3. 再次运行 `scripts/build_docx.py` —— 此时脚本检测到 `toc_pages.json`，把目录页码写成**静态文本**，所有阅读器都能正确显示。

```powershell
& "C:\Users\a'su's\.workbuddy\binaries\python\envs\office\Scripts\python.exe" "<skill目录>\scripts\compute_pages.py"
& "C:\Users\a'su's\.workbuddy\binaries\python\envs\office\Scripts\python.exe" "<skill目录>\scripts\build_docx.py"
```

`compute_pages.py` 中的文档路径常量需指向本次输出的 docx（先把 build 出的文件改名或改常量）。

### 第 5 步：校验与交付

- 导出 PDF 用 `fitz`（PyMuPDF）逐页抽取文本，核对目录页码与 PDF 实际页面对应；`verify_toc.py` 已实现该逻辑（可复制到工作目录按需改名运行）。
- 全文字色校验：解析 `word/document.xml` 确认无 `w:color` 非黑色（除黑色 000000）。
- 用 `grep`/`Grep` 全文搜索易漏项（"宿舍""专家论证""占位"等）确认已清零。
- 交付时 `present_files` 打开预览，并提醒用户：封面签名、编制/审核/批准姓名需手写；附图若为占位需替换。

## 环境

- Python venv：`C:\Users\a'su's\.workbuddy\binaries\python\envs\office\Scripts\python.exe`（已装 python-docx、pywin32、PyMuPDF）。无则先 `python -m venv` 建一个并 `pip install python-docx pywin32 pymupdf`。
- 构建与 Word COM 操作在 Windows + 已装 Microsoft Word 环境下运行（compute_pages.py 依赖 win32com + Word）。
- Bash 沙箱在本机不稳定时，改用 **PowerShell** 执行 python 脚本，并把输出重定向到 `.txt` 再用 Read 读取。
- 中文字体直接用 `"宋体"`/`"黑体"`（Word 内部英文名），无需额外字体文件。

## 常见调整与坑（来自实战）

- **在线预览页码全为"2"** → 见第 4 步，必须写死静态页码。
- **"全黑字"要求** → `set_cjk` 默认黑色，Normal/Heading 样式也显式设黑色，删除所有 `color=RGBColor(红)`。
- **路线图是手机长截图，被挤到下一页** → 宽度压到 8cm 并给"附件6"标题加 `page_break_before` + `keep_with_next`。
- **"现场无宿舍"** → 删除所有"宿舍"表述，改"现场办公区/临时休息区"；危险源"临时建筑"去掉宿舍。
- **"无超危大工程"** → 删除"专家论证"等字样。
- **压缩页数到 40 页内** → 正文 10.5pt、行距 1.25、段后 3pt、表格 10pt、图片 14→11cm。
- **目录与编制依据分页** → 封面后 `---` 插入目录并 `add_page_break()`；"一、编制依据"起始即新页。
